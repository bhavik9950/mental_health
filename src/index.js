import React from 'react';
import ReactDOM from 'react-dom/client';
import './health.css';
import Help from './Crowdsourced';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <Help />
  </React.StrictMode>
);